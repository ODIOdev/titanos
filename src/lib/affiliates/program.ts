/**
 * Shared affiliate program constants. Kept dependency-free so client
 * components can import it without pulling in server-only data modules.
 */

/** Completed orders a customer needs before an application can be approved. */
export const AFFILIATE_ELIGIBILITY_ORDERS = 10;
